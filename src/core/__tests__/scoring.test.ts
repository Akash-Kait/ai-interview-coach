import { describe, it, expect } from 'vitest';

// Placeholder proving the toolchain (TypeScript + Vitest) runs.
// Build-sequence step 2 replaces this with real scoring unit tests.
describe('toolchain smoke', () => {
  it('runs TypeScript unit tests', () => {
    expect(1 + 1).toBe(2);
  });
});
